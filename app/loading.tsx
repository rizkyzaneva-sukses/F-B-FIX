export default function Loading() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", color: "#64748b", fontFamily: "Plus Jakarta Sans, sans-serif" }}>
      Memuat ruang kerja DapurKasir...
    </main>
  );
}
