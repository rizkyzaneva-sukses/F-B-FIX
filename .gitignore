node_modules
.next
.env
.env.local
*.log
tsconfig.tsbuildinfo
data/

# Repo memakai npm (package-lock.json) untuk build Docker. Lockfile pnpm lokal tidak
# boleh ikut ter-commit: Next.js akan mendeteksi pnpm sebagai package manager lalu
# memanggil "pnpm config get registry" di dalam image yang tidak punya pnpm.
pnpm-lock.yaml
pnpm-workspace.yaml
